import { SettingsPageFrame } from "@/components/settings/settings-shell";

import { NewProxyForm } from "../new-proxy-form";

const NewProxyPage = () => (
  <SettingsPageFrame>
    <NewProxyForm />
  </SettingsPageFrame>
);

export default NewProxyPage;
