# Builds the image from this checkout instead of pulling the release image.
# Used by install.sh with PURR_BUILD=1 (CI e2e) and when a pull is impossible.
services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
